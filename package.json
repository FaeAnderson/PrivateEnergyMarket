{
  "name": "private-energy-market",
  "version": "1.0.0",
  "description": "Private Energy Market - Anonymous Energy Trading using FHE",
  "main": "index.html",
  "scripts": {
    "start": "npx http-server public -p 8080 -c-1 --cors",
    "build": "mkdir -p public && cp index.html public/ && cp app.js public/ && cp styles.css public/ && cp favicon.ico public/ && cp -r contracts public/ && cp *.png public/ && cp *.mp4 public/ && cp *.html public/",
    "dev": "npx http-server public -p 3000 -c-1 --cors"
  },
  "keywords": [
    "energy",
    "blockchain",
    "privacy",
    "fhe",
    "trading"
  ],
  "author": "Private Energy Market",
  "license": "MIT",
  "dependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}